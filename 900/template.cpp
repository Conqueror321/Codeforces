#include <iostream>

#define pb push_back
#define vt vector
#define REP(n) for(int i = 0; i < n; i++)
#define ll long long
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int t;
    cin >> t;
    while(t--) {
        
    }
}